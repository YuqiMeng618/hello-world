$ git add
$ git commit -m

$ git push
push committed changes to the remote 

$ touch
create a new file

$ git log
show the history (all the commit)
allow you to go back to any previous changes

$ git reset --hard <harsh #>

Undo changes
before you add 
$ git checkout -- filename

after add (already in the staging area)
$ git reset filename
then use git checkout again to delete you change

after you commit
$ git revert harshNumber
$ git revert -n harshNumber   # without commit 

create a new branch
$ git branch student1

!!change the branch
$ git checkout student1

$ git push origin studnet1
change only made to student1

delete branch #Be careful you may lose you did not merge to the master, you'll lose all your changes
two ways
$ git push -- delete origin

Debug the code
%xmode Plain
%xmode Verbose
%xmode Context

Debugger
import pdb; pdb.set.trace()
%pdb on

# count and measure the time
%time 
# gives detail information about yoru function
%prun func



